# Publish a tagged release to PyPI. Nothing else in this repository publishes,
# and nothing in this file runs on a branch — a release is a tag, deliberately,
# so that publishing is an act with a name rather than a side effect of a merge.
#
# Trusted publishing (OIDC) rather than an API token: the runner exchanges a
# short-lived identity for an upload, so there is no long-lived secret in this
# repository to store, leak or rotate. The trade is that PyPI has to be told
# which workflow in which environment it trusts, which is configuration that
# lives there and not here. Both sides of that trust have to exist before the
# first tag — the publisher on PyPI and the `pypi` environment in this
# repository's settings. VERSIONING.md § Releasing has the two of them.
name: Release

on:
  push:
    tags: ["v*"]

# Read-only by default. Only the publish job widens this, and only to the one
# permission the OIDC exchange needs.
permissions:
  contents: read

# Deliberately NOT `cancel-in-progress`, which is what ci.yml sets. Cancelling
# a CI run wastes a runner; cancelling a run that is mid-upload can leave a
# release half-landed, and a PyPI version number can never be reused — not even
# after deleting the file. There is nothing to gain here by being fast.
concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  # Everything that can say no, said before anything leaves the runner.
  build:
    runs-on: ubuntu-latest
    # A fork can push a tag to itself; it must not be able to build the
    # artifacts this repository's trust policy would then be asked to publish.
    if: github.repository == 'sigrix-io/sigrix-mcp'
    steps:
      # checkout and setup-python track ci.yml's pins on purpose, rather than
      # the newest majors: this workflow runs once per release, at the moment
      # when discovering an action's breaking change is most expensive, so it
      # should run on the versions the rest of the repository has exercised.
      # Dependabot moves both files together.
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.13"

      - run: python -m pip install build twine
      - run: python -m build

      # hatchling reads the version out of pyproject.toml; the tag is typed by
      # hand. Nothing makes them agree. Tagging v0.3.0 without bumping
      # pyproject builds 0.2.0 and would publish it under a tag saying
      # otherwise — or, if 0.2.0 is already on PyPI, fail at the upload with a
      # duplicate-file error naming neither cause. Ask here instead, where the
      # answer is still cheap.
      #
      # Read off the built wheel rather than out of pyproject: what shipped is
      # the thing the tag has to name, and those are only the same file until
      # the build does something unexpected.
      - name: The tag names the version that was actually built
        run: |
          set -euo pipefail
          tag="${GITHUB_REF_NAME#v}"
          # sigrix_mcp-0.2.0-py3-none-any.whl -> 0.2.0. A wheel filename uses
          # "-" as its field separator, so a PEP 440 version can never contain
          # one.
          built="$(basename "$(ls dist/*.whl)" | cut -d- -f2)"
          echo "tag=${tag}  built=${built}"
          if [ "${tag}" != "${built}" ]; then
            echo "::error::tag ${GITHUB_REF_NAME} does not name the packaged version ${built}. Bump pyproject.toml, or delete the tag and retag."
            exit 1
          fi

      # A release nobody wrote down is one a dependent cannot evaluate, which
      # is the whole promise VERSIONING.md makes while this is 0.x. Keep a
      # Changelog puts the version in a heading, so both halves below are exact
      # checks rather than guesses at prose.
      #
      # The second half exists because the first is not enough on its own: a
      # heading can be present and correct while shipped changes still sit
      # under [Unreleased], and a heading check cannot see that. Anything still
      # filed as unreleased is on the commit being tagged, so it goes out in
      # this release while the changelog says it did not.
      - name: The changelog names this version, and nothing is left unreleased
        run: |
          set -euo pipefail
          export VERSION="${GITHUB_REF_NAME#v}"
          python - <<'PY'
          import os
          import pathlib
          import re

          version = os.environ["VERSION"]
          text = pathlib.Path("CHANGELOG.md").read_text(encoding="utf-8")


          def fail(message: str) -> None:
              # stdout, not stderr: that is where GitHub reads ::error:: from.
              print(message)
              raise SystemExit(1)


          if f"## [{version}]" not in text:
              fail(
                  f"::error::CHANGELOG.md has no '## [{version}]' heading. Add it, or "
                  "move the Unreleased entries under it, before tagging."
              )

          # The heading itself stays -- Keep a Changelog keeps it between
          # releases. Only its contents are wrong at tag time.
          section = re.search(
              r"^##\s*\[Unreleased\]\s*$(.*?)(?=^##\s|\Z)",
              text,
              re.MULTILINE | re.DOTALL,
          )
          if section:
              leftover = [
                  line
                  for line in section.group(1).splitlines()
                  if line.strip() and not line.lstrip().startswith("#")
              ]
              if leftover:
                  fail(
                      f"::error::CHANGELOG.md still files {len(leftover)} line(s) under "
                      f"[Unreleased], starting {leftover[0].strip()[:60]!r}. They are on "
                      f"the commit being tagged, so they ship in {version} -- move them "
                      f"under '## [{version}]' before tagging."
                  )
          PY

      # Catches a README that renders on GitHub and not on PyPI, which is
      # otherwise found at the moment of publishing and not before.
      - run: python -m twine check dist/*

      # ci.yml proves the *source tree* passes, via an editable install. This
      # proves the *artifact* does, which is a different question and the only
      # one that still matters once the bytes are immutable on PyPI: a file
      # missing from the wheel is invisible to every editable-install run, and
      # this package ships two that are not Python — `server.py` reads
      # `seller_api_openapi.json` through `importlib.resources`, and `py.typed`
      # is what makes the `Typing :: Typed` classifier mean anything.
      - name: The built wheel installs, imports, and passes the suite
        run: |
          set -euo pipefail
          python -m venv /tmp/releasecheck
          /tmp/releasecheck/bin/python -m pip install --quiet dist/*.whl pytest
          # From /tmp, so nothing in the checkout can satisfy the import.
          cd /tmp
          # A quoted heredoc, so the shell does not touch a character of this:
          # unquoted, backticks and $ in Python source are command
          # substitutions, and the failure mode is silent -- the shell deletes
          # the text and hands Python something that still parses.
          /tmp/releasecheck/bin/python - <<'PY'
          import asyncio
          import pathlib
          import sigrix_mcp
          import sigrix_mcp.server as s

          # pyproject declares `Typing :: Typed`, which a consumer's checker
          # only honours if the marker actually shipped.
          root = pathlib.Path(sigrix_mcp.__file__).parent
          assert (root / "py.typed").exists(), "py.typed did not ship"

          names = sorted(t.name for t in asyncio.run(s.server.list_tools()))
          assert names, "the server lists no tools"
          print(sigrix_mcp.__version__, "-", len(names), "tools resolve")
          PY
          cd "${GITHUB_WORKSPACE}"
          # src-layout means ./sigrix_mcp does not exist, so the import below
          # can only resolve to the wheel in the venv. Asserted rather than
          # assumed: a suite that quietly re-tested the source tree would look
          # identical.
          /tmp/releasecheck/bin/python - <<'PY'
          import sigrix_mcp

          assert "site-packages" in sigrix_mcp.__file__, sigrix_mcp.__file__
          PY
          /tmp/releasecheck/bin/python -m pytest tests/ -q

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: dist
          path: dist/
          if-no-files-found: error

  # Separate from the build so that a failure above cannot half-publish: this
  # job does not start unless every check passed, and it builds nothing itself.
  publish:
    needs: build
    runs-on: ubuntu-latest
    environment:
      # PyPI's trusted-publisher policy names this environment alongside the
      # repository and the workflow filename, so the three have to match what
      # is configured there. It is also where to add a required reviewer if a
      # release should ever need a second pair of eyes.
      name: pypi
      url: https://pypi.org/p/sigrix-mcp
    permissions:
      # The only elevated permission in this file, scoped to the one job that
      # needs it. This mints the OIDC token PyPI exchanges for an upload, and
      # it is also what makes the action attach PEP 740 attestations.
      id-token: write
    steps:
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: dist
          path: dist/

      # Pinned to the commit rather than to `release/v1`, which is a *branch*:
      # it moves whenever upstream pushes, and nothing in this repository would
      # record that it had. That matters here and nowhere else in this file —
      # this is the one step holding upload rights to PyPI, through the OIDC
      # token the permission above mints. `tests/test_release_workflow.py`
      # fails if this goes back to a tag or a branch, and Dependabot rewrites
      # the trailing comment when it bumps the pin.
      - uses: pypa/gh-action-pypi-publish@dc37677b2e1c63e2034f94d8a5b11f265b73ba33 # v1.14.2

  # An upload that succeeds is not the same as a package anyone can install,
  # and the two failures look identical from the publish step: a green tick.
  # This is the check that tells them apart — install by name, from the index
  # the upload just went to, the way a user would.
  #
  # What it catches, none of which the publish step can see: an upload that
  # did not land, an index that will not serve it, a wheel whose schema or
  # type marker did not ride along, and a version that disagrees with the one
  # this run built. The build job proves those properties of a *local* file;
  # this proves them of the artifact the world actually gets.
  #
  # Adapted from `sigrix-io/postern`, which has run this shape through real
  # releases. The one deliberate difference is the retry window, below.
  verify:
    needs: [build, publish]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.13"

      - name: Install from PyPI by name, and run what was published
        run: |
          set -euo pipefail

          # The build job already refused the tag unless it named the packaged
          # version, so the tag is a sound source for what to expect here.
          EXPECTED="${GITHUB_REF_NAME#v}"

          python -m venv /tmp/verify

          # VERSIONING.md tells a reader to allow about ten minutes after an
          # upload before expecting the new version to resolve, because of the
          # index's CDN cache. A check that gave up sooner than the
          # documentation says to wait would report a healthy release as a
          # failure, so this waits out the whole documented window: 40 attempts
          # at 15 seconds is ten minutes. It breaks on the first success, so
          # the usual cost is one attempt — the window only matters when
          # something is actually wrong, and that is not a case worth being
          # fast about.
          for attempt in $(seq 1 40); do
            if /tmp/verify/bin/pip install --quiet "sigrix-mcp==$EXPECTED"; then
              echo "installed on attempt $attempt"
              break
            fi
            if [ "$attempt" = "40" ]; then
              echo "::error::sigrix-mcp==$EXPECTED never became installable from PyPI. The upload reported success, so check the project page before re-tagging — a version is spent permanently either way."
              exit 1
            fi
            echo "not on the index yet (attempt $attempt); waiting"
            sleep 15
          done

          # Not merely that something installed — that the thing published by
          # *this run* did. An older release already on the index would
          # satisfy a bare install and tell us nothing.
          cd /tmp
          EXPECTED="$EXPECTED" /tmp/verify/bin/python - <<'PY'
          import asyncio
          import os
          import pathlib
          from importlib.metadata import entry_points

          import sigrix_mcp
          import sigrix_mcp.server as s

          expected = os.environ["EXPECTED"]
          assert sigrix_mcp.__version__ == expected, (
              f"installed {sigrix_mcp.__version__} from the index, expected {expected}"
          )

          root = pathlib.Path(sigrix_mcp.__file__).parent
          assert (root / "py.typed").exists(), "py.typed did not survive the round trip to PyPI"
          assert (root / "seller_api_openapi.json").exists(), "the schema did not survive the round trip to PyPI"

          names = sorted(t.name for t in asyncio.run(s.server.list_tools()))
          assert names, "the published package lists no tools"

          script = [e for e in entry_points(group="console_scripts") if e.name == "sigrix-mcp"]
          assert script, "the sigrix-mcp console script is not in the published distribution"
          assert script[0].load().__name__ == "main"

          print(f"{expected} installs from PyPI and serves {len(names)} tools:", names)
          PY
